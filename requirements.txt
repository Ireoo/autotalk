# 必需依赖
numpy==1.26.3
loguru==0.7.2
requests==2.31.0
tqdm==4.66.1
pyperclip==1.8.2

# 音频处理 (可选，但推荐安装)
# Windows用户安装PyAudio:
# 方法1: pip install pipwin && pipwin install pyaudio
# 方法2: 下载适合你Python版本的wheel文件: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
#      然后使用 pip install 下载的wheel文件路径
# 方法3: pip install PyAudio

# Linux/Mac用户:
pyaudio==0.2.13; sys_platform == 'linux' or sys_platform == 'darwin'

# 高级音频处理库 (可选)
soundfile==0.12.1
librosa==0.10.1

# GUI依赖 (可选)
# 如果不安装，程序将使用命令行界面
PyQt6==6.6.1

# 语音识别 (可选，推荐安装以下之一)
# 选项1: whisper-cpp-python (C++实现，更高性能)
# Windows用户可能需要安装Visual C++ 构建工具: https://visualstudio.microsoft.com/visual-cpp-build-tools/
# pip install whisper-cpp-python

# 选项2: 原始OpenAI whisper (纯Python实现，安装简单但速度较慢)
# pip install openai-whisper

# 安装所有依赖的简便方法:
# pip install PyAudio PyQt6 whisper-cpp-python
# 或者
# pip install PyAudio PyQt6 openai-whisper 